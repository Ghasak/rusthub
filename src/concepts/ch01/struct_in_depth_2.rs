pub fn detecting_new_struct_initialization() {

    /*!
    Because we need the `lazy_static` crate, you need to add the following to your
    `Cargo.toml` file:

    ```cargo
    [dependencies]
    lazy_static = "0.2.1"
    ```
    */
}
